<% content_for(:title) do %>
    Request an Invitation
<% end %>

<h2 class="subhead"><span>Request an Invitation</span></h2>

<p>Privly is under active development. To get invited when we have a major release, enter your email below. Existing users can <%= link_to "Sign in", new_user_session_path %>.</p>

<br/>

<%= form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :post} do |f| %>
  <%= devise_error_messages! %>

  <p><%= f.label :email %><br />
  <%= f.text_field :email %></p>

  <p><%= f.submit "Sign up for an invitation" %></p>
<% end %>

<br />

<h2>Development user accounts are open until 9 PM PDT (UTC -7). <%= link_to "Register", new_registration_path(resource_name) %></h2>
